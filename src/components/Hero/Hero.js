import React from 'react';

import NavBar from 'components/NavBar/NavBar';

import { StyledWrapper } from './styled';

const Hero = () => {
  return (
    <StyledWrapper>
      <NavBar />
    </StyledWrapper>
  );
};

export default Hero;
